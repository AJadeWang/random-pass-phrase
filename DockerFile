FROM php:8.4-cli

# Set working directory
WORKDIR /app

# Copy your PHP files
COPY src/ /app/

# Expose port 8080
EXPOSE 8080

# Start PHP built-in server
CMD ["php", "-S", "0.0.0.0:8080", "-t", "/app"]
